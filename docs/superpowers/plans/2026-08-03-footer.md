# playground footer — implementation plan

Spec: `docs/superpowers/specs/2026-08-03-footer-design.md`
(source of truth; its Locked decisions are binding).

## Task 1: footer

- [x] In `textkit/web.py` add `<footer id="footer">textkit playground ·
  {N} operations</footer>` after the result paragraph, with N computed from
  `len(OPERATIONS)` when the page is rendered. No other markup or endpoint
  changes.
- [x] Update the page-markup unit tests in `tests/test_web.py`: the rendered
  page contains `id="footer"` and `5 operations`; existing tests stay green.
  Fold the `CHANGELOG.md` entry into the same commit (changelog skill rule 4).
- [x] Run `python3 -m unittest discover -s tests -v` — green.
