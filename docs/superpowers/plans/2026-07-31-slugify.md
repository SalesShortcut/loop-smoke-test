# План: slugify

**Спека:** `docs/superpowers/specs/2026-07-31-slugify-design.md`

### Task 1: Тесты slugify

- [x] Создать `tests/test_slugify.py` с тестами по примерам спеки
      (базовый случай, пунктуация, цифры, пустая строка, только-кириллица,
      обрезка дефисов).
- [x] Прогнать `python3 -m unittest discover -s tests -v` — новые тесты падают
      (ImportError), старые проходят.

### Task 2: Реализация

- [x] Добавить `slugify` в `textkit/core.py` (только stdlib, регулярные
      выражения `re` разрешены).
- [x] Экспортировать `slugify` из `textkit/__init__.py`.
- [x] Прогнать `python3 -m unittest discover -s tests -v` — всё зелёное.

### Task 3: Документация

- [x] Дополнить README.md разделом об использовании `slugify` с примером.
