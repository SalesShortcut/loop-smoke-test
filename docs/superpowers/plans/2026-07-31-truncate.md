# truncate() Implementation Plan

**Goal:** добавить `truncate(text, width)` в textkit по спеке `docs/superpowers/specs/2026-07-31-truncate-design.md`.

Выполнять задачи по порядку, код брать из блоков ниже как есть. После каждой задачи — коммит.

### Task 1: Тесты

- [x] В `tests/test_core.py` добавить:

```python
class TruncateTests(unittest.TestCase):
    def test_short_text_unchanged(self):
        self.assertEqual(truncate("hi", 10), "hi")

    def test_exact_width_unchanged(self):
        self.assertEqual(truncate("hello", 5), "hello")

    def test_long_text_gets_ellipsis(self):
        self.assertEqual(truncate("hello world", 8), "hello w…")
        self.assertEqual(len(truncate("hello world", 8)), 8)
```

Импорт дополнить: `from textkit import truncate, word_count`.

- [x] Прогнать `python3 -m unittest discover -s tests -v` — новые тесты падают (ImportError).

> Дополнено сверх блока кода плана: тест `test_non_positive_width_raises` —
> требование 3 спеки (`ValueError("width must be positive")` при `width <= 0`).

### Task 2: Реализация

- [x] В `textkit/core.py` добавить (с валидацией `width` по требованию 3 спеки
  и секцией `Example:` по правилам CLAUDE.md):

```python
def truncate(text: str, width: int) -> str:
    """Truncate text to width chars, ending with an ellipsis when cut."""
    if len(text) <= width:
        return text
    return text[: width - 1] + "…"
```

- [x] В `textkit/__init__.py`:

```python
from .core import truncate, word_count

__all__ = ["truncate", "word_count"]
```

- [x] Прогнать тесты — все зелёные.

### Task 3: Changelog

- [x] Обновить `CHANGELOG.md` по правилам репозитория (скилл changelog).
  Запись вошла в коммит фичи (Task 2) — правило 4 скилла запрещает отдельный
  коммит для changelog.
- [ ] Финальный прогон `python3 -m unittest discover -s tests -v`.
