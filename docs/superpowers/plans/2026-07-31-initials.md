# План: initials

**Спека:** `docs/superpowers/specs/2026-07-31-initials-design.md`

Перед началом прочитай `CLAUDE.md` в корне репозитория и следуй его правилам.

### Task 1: Тесты initials

- [x] Создать `tests/test_initials.py` с тестами по примерам спеки
      (обычное имя, лишние пробелы, пустая строка, однобуквенные слова).
- [x] Прогнать `python3 -m unittest discover -s tests -v` — новые тесты
      падают, старые проходят.

### Task 2: Реализация

- [x] Добавить `initials` в `textkit/core.py`.
- [x] Экспортировать из `textkit/__init__.py`.
- [x] Прогнать `python3 -m unittest discover -s tests -v` — всё зелёное.

### Task 3: Документация

- [ ] Дополнить README.md разделом про `initials`.
